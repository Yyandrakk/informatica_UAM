#ifndef TYPES_H
#define	TYPES_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef enum {
    FALSE, TRUE
} BOOL;

typedef enum {
    ERROR, OK
} STATUS;

#endif	/* TYPES_H */
