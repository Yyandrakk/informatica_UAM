/* 
 * File:   type.h
 * Author: Oscar y Maria
 * Pareja: 6
 */

#ifndef TYPES_H
#define	TYPES_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef enum {FALSE = 0 , TRUE = 1} BOOL;
typedef enum {ERROR = -1, OK = 0} STATUS;

#endif	/* TYPES_H */
